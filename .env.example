# Event Horizon Gallery - Environment Variables

# Iron secret for token signing (must be at least 32 characters)
IRON_SECRET="replace-with-a-long-random-secret"

# Postgres URL used by runtime queries (pooled URL is fine)
DATABASE_URL="postgresql://user:password@host:5432/event_horizon?sslmode=require"

# Postgres URL used by Prisma Migrate (direct/non-pooled URL recommended)
DIRECT_URL="postgresql://user:password@host:5432/event_horizon?sslmode=require"

# SMTP configuration (required for contact form and admin passkey email)
SMTP_HOST="smtp.example.com"
SMTP_PORT="587"
SMTP_USER="user@example.com"
SMTP_PASS="replace-with-smtp-password"
SMTP_FROM="noreply@example.com"

# Support Dock - PayPal & Crypto (optional)
NEXT_PUBLIC_PAYPAL_DONATE_URL=""
NEXT_PUBLIC_BTC_ADDRESS=""
NEXT_PUBLIC_BCH_ADDRESS=""
